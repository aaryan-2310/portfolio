{
    "version": 2,
    "buildCommand": "yarn build",
    "outputDirectory": "dist/browser",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ]
}